# Sanity client

Phase 7 fills this folder with the Sanity client, draft-mode helpers, and
the `/api/revalidate` route handler that the backend's `hurc-sanity-bridge`
plugin posts to. The cache-tag namespace shared with `lib/vendure/revalidation.ts`
is reused.
