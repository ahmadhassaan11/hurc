/**
 * @hurc/ui — design system entrypoint.
 *
 * Phase 1 stub. Phase 5 populates `primitives/` (Button, Input, Sheet, ...) and
 * higher-order components (PriceTag, ColorSwatch, FreeShippingProgress, ...).
 */

export const PACKAGE_NAME = '@hurc/ui' as const;
