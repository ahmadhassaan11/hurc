/**
 * @hurc/testing — shared test utils, MSW handlers, fixtures.
 *
 * Phase 1 stub. Phase 9 fills with: render helpers, MSW handlers for the
 * Vendure shop-api, product/customer/order fixtures.
 */

export const PACKAGE_NAME = '@hurc/testing' as const;
