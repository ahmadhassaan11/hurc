/**
 * @hurc/utils — shared, framework-agnostic helpers.
 *
 * Phase 1 stub. Real implementations land in later phases:
 *   - money:     Intl.NumberFormat formatting + minor-unit math
 *   - locale:    locale ↔ channel/currency mapping
 *   - cookies:   typed cookie helpers used across server actions
 *   - slug:      product/collection slugify
 */

export const PACKAGE_NAME = '@hurc/utils' as const;
