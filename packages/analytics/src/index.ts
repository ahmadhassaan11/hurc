/**
 * @hurc/analytics — Plausible (always-on, cookieless) + PostHog (consent-gated).
 *
 * Phase 1 stub. Phase 4 wires concrete clients + the Klaro consent bridge.
 */

export const PACKAGE_NAME = '@hurc/analytics' as const;
